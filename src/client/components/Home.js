import React from "react";

const Home = () => {
  return (
    <div>
      <button onClick={() => console.log("ssss")}>click</button>
      <div>I'm the home componens</div>
    </div>
  );
};

export default Home;
